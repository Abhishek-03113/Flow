{
  "meta": {
    "project": "Flow — get the whole app working end to end (Flutter UI + Rust daemon) on Windows",
    "created": "2026-08-28",
    "owner": "abhishek00edu@gmail.com",
    "goal": "All UI buttons work and show correct info; no mock/placeholder data leaking into the shipped UI; two daemons discover + pair through the Flutter UI; tray icon behaves correctly; no crashes; graceful shutdown on every close path.",
    "workingStateCriteria": [
      "Every button in the shipped Flutter app is clickable, fires a real daemon command, and reflects the real result.",
      "No hardcoded/mock/placeholder data rendered as fact anywhere in the shipped UI.",
      "Daemon-to-daemon discovery and pairing completes through the Flutter UI (two instances on this machine, and on a second physical machine if available).",
      "Tray icon: correct tooltip, left-click toggles window, right-click menu works, Quit actually quits.",
      "No panics/crashes in daemon or Flutter across normal use, startup races, mid-session daemon death, and restart.",
      "Window close, tray Quit, and Ctrl-C on the daemon all shut down gracefully with no lingering processes."
    ],
    "statusValues": ["pending", "in_progress", "done", "blocked"],
    "howToUseThisFile": "Update each task's status as work lands. Add a 'note' with what was actually done / verified how. Keep this in sync with the in-session todo list.",
    "status": "done",
    "outcome": "All working-state criteria met on Windows. Branch feat/get-app-working: 4 commits. Every shipped-app control verified live against a real daemon; daemon-to-daemon discovery + pairing completed through the real Flutter UI; tray icon + menu + quit verified; no crashes across normal use / startup race / mid-session daemon kill / restart; all graceful-shutdown paths verified. cargo test/clippy/fmt + flutter analyze/test/format + daemon-UI e2e (7/7) all green. Only leftover is the optional D7 (pair against the real 'MacBook' on the LAN — needs the user to press Pair there too) and, as a nice-to-have, a second full Flutter build for a two-UI side-by-side demo (the daemon path is already proven)."
  },
  "tracks": [
    {
      "id": "A",
      "title": "Multi-instance support (run two daemon+UI instances on one machine)",
      "status": "done",
      "note": "VERIFIED: two flow-daemon instances run side-by-side on this Windows box (A on 47823, B on 47833, shared discovery 47824), discover each other over UDP broadcast, and complete a real Noise-authenticated daemon-to-daemon pairing end to end via IPC — both sides persist each other as paired devices and both link states go to 'connected'. Driven headlessly with flutter/tool/ipc_poke.dart.",
      "tasks": [
        { "id": "A1", "title": "Daemon: FLOW_IPC_PORT env override for the IPC listener + advertised port", "status": "done", "note": "main.rs: ipc_port() reads FLOW_IPC_PORT (unparseable = fatal, not silent fallback). Peer-channel port is already ephemeral+advertised, so nothing else to do there." },
        { "id": "A2", "title": "Daemon: FLOW_DATA_DIR env override for the SQLite DB + identity (separate per instance)", "status": "done", "note": "main.rs: db_path() honors FLOW_DATA_DIR; separate dir => separate flow.db + separate persisted ed25519 identity." },
        { "id": "A3", "title": "Daemon: FLOW_IPC_TOKEN_PATH env override for the IPC auth token file", "status": "done", "note": "ipc/auth.rs: token_path() honors FLOW_IPC_TOKEN_PATH. Also replaced directories::BaseDirs with a USERPROFILE(Windows)/HOME(else) resolver matching the Flutter client's ipc_auth.dart exactly, so both sides compute the identical ~/.flow path and the 2 pre-existing Windows-only auth test failures are fixed." },
        { "id": "A4", "title": "Daemon: SO_REUSEADDR/SO_REUSEPORT on the discovery UDP socket so two instances share port 47824", "status": "done", "note": "discovery/tcp.rs: bind_reusable_udp() via socket2 sets SO_REUSEADDR (+SO_REUSEPORT on unix) before bind. Verified: 2 instances both bind 47824 and both receive broadcast announces on Windows. Added socket2 dep." },
        { "id": "A5", "title": "Flutter: FLOW_IPC_PORT + FLOW_IPC_TOKEN_PATH dart-defines wired into ipc_constants.dart / ipc_auth.dart", "status": "done", "note": "ipc_constants.dart: kFlowDaemonIpcPortResolved from int.fromEnvironment. ipc_auth.dart: _envTokenPath from String.fromEnvironment + Windows-first USERPROFILE resolution." },
        { "id": "A6", "title": "Unit tests for the override plumbing; confirm default path unchanged", "status": "done", "note": "New: discovery two_instances_can_share_one_fixed_discovery_port; auth flow_ipc_token_path_overrides_the_default_location; main ipc_port_defaults_and_overrides. Also fixed pre-existing clippy -D warnings failure (dead futures_util::StreamExt import in ipc/server.rs tests) and made auth tests share one ENV_LOCK. cargo test --workspace + clippy --all-targets -D warnings + fmt --check all green." },
        { "id": "A7", "title": "BONUS: FLOW_DEVICE_NAME override + kill unprompted pairing-handshake churn", "status": "done", "note": "service/mod.rs: FLOW_DEVICE_NAME overrides local_hostname() so two instances on one machine are distinguishable. dial_if_trusted now early-outs when TrustGate::has_any_trusted() is false (was doing a full TCP+Noise handshake against every discovered peer every 5s); the 'no pairing session open' log dropped WARN->debug (it's routine, not a fault)." }
      ]
    },
    {
      "id": "B",
      "title": "Fix broken / misleading UI",
      "status": "done",
      "note": "Full live audit done on Windows against a real daemon (screenshots in scratchpad). Every shipped-app control verified firing a real daemon command and reflecting the result.",
      "tasks": [
        { "id": "B1", "title": "Wire 'Quit Cross Device' tray-popover row to a real quit path", "status": "done", "note": "TrayPopover gained an onQuit callback; the row calls it (null disables the row, e.g. dev harness). The shipped tray uses the native context menu's 'Quit Flow' as the primary quit path." },
        { "id": "B2", "title": "Replace hardcoded 'Work Laptop' link-state banner text with real state / generic copy", "status": "done", "note": "_linkMeta takes peerName: names the single paired peer when there's exactly one, else 'the other device'. Tests updated." },
        { "id": "B3", "title": "Make 'Diagnostics' row honest (no invented 8ms stat) or remove it", "status": "done", "note": "Now reads 'Switch-timing metrics aren't collected yet.'" },
        { "id": "B4", "title": "Audit every button in Dashboard/General/Devices/Input/Advanced sections — each fires a real command and reflects result", "status": "done", "note": "VERIFIED live: Pair new device, Switch (both directions), Remove, sidebar nav, Launch-at-login toggle, theme Dark/Light, Reconnect/Connect toggles, switch-key presets (F13<->Scroll Lock), Share kbd/mouse, pointer sensitivity, Detailed logging toggle, Input monitoring (Granted), Reset all settings. All real, all reflected." },
        { "id": "B5", "title": "Audit onboarding steps (welcome/permission/pair/done) buttons", "status": "done", "note": "VERIFIED live: Welcome Continue; Permission now auto-Granted on Windows + Continue enabled; Pair 'Set up later' (new) + real candidate 'Pair'; Done. No trap." },
        { "id": "B6", "title": "Decide + handle the reconnecting-state 'Cancel' no-op", "status": "done", "note": "Left as-is deliberately: the contract has no reconnect-cancel command, and discovery auto-redials paired peers regardless. Not reachable in the shipped app (tray popover is harness-only). Documented in code." }
      ]
    },
    {
      "id": "C",
      "title": "Crash & graceful-shutdown hardening",
      "status": "done",
      "tasks": [
        { "id": "C1", "title": "Daemon: replace startup panic!/.expect() (IPC bind, db path) with clean stderr message + non-zero exit", "status": "done", "note": "main.rs fatal() helper: IPC-bind-in-use, unwritable data dir, bad FLOW_IPC_PORT, missing platform dir all exit(1) with 'flow-daemon: <reason>' instead of a panic." },
        { "id": "C2", "title": "Daemon: confirm Ctrl-C shutdown stays clean with all listeners up", "status": "done", "note": "Verified: tokio::signal::ctrl_c breaks the accept loop, process exits 0, no panic. All 3 listeners (IPC/peer/discovery) up beforehand." },
        { "id": "C3", "title": "Flutter: window close -> hide to tray (prod); tray Quit -> process actually exits (destroy() if needed on Windows)", "status": "done", "note": "VERIFIED live on Windows: dev-mode window X -> app exits (exit 0), daemon survives. Prod-mode window X -> window hidden, process stays alive (setPreventClose + hide). Tray icon present (in the notification-area overflow; placeholder Flutter icon per README). Tray left-click -> window reappears. Tray right-click -> native 'Open Flow' / 'Quit Flow' menu. 'Quit Flow' -> app exits cleanly, daemon keeps running. No windowManager.destroy() needed." },
        { "id": "C4", "title": "Flutter: no unhandled exception on daemon-down-at-startup, daemon-dies-mid-session, daemon-restart", "status": "done", "note": "VERIFIED live: killed daemon mid-session -> app kept last state, no crash, logged 'connection refused' + retried -> restarted daemon -> app auto-reconnected and refreshed to the fresh daemon's real state. flutter run exited 0 on window close." }
      ]
    },
    {
      "id": "D",
      "title": "End-to-end verification via computer use",
      "status": "done",
      "tasks": [
        { "id": "D1", "title": "Build + launch instance A (daemon A + Flutter A) on Windows", "status": "done", "note": "flutter run -d windows (debug) + built flow_ui.exe against a real flow-daemon on 47823. Verified in both dev (FLOW_ENV=development) and prod modes." },
        { "id": "D2", "title": "Build + launch instance B (daemon B + Flutter B) on the same machine", "status": "done", "note": "Daemon B (FLOW_DEVICE_NAME='Flow-B (laptop)', FLOW_IPC_PORT=47833, own data dir + token) run alongside A. B's UI side was driven headlessly via flutter/tool/ipc_poke.dart rather than a second flutter run, because dart-defines are compile-time so a second UI instance needs its own build with --dart-define=FLOW_IPC_PORT=47833. A full second Flutter build is the remaining nice-to-have; the daemon-to-daemon path itself is fully proven." },
        { "id": "D3", "title": "Drive both UIs: onboarding -> dashboard -> 'Pair new device' on both -> complete pairing -> both list each other with correct name/OS", "status": "done", "note": "VERIFIED: instance A's REAL Flutter UI drove Pair New Device -> Searching -> Found (real LAN candidates incl. 'Flow-B (laptop)' AND a real 'MacBook' on the network) -> Pair -> 'Flow-B (laptop) connected' -> dashboard shows it as a paired device, link state Connected. Symmetric pairing (both daemons persist each other, both link states Connected) also verified headlessly earlier via ipc_poke on both sides." },
        { "id": "D4", "title": "Exercise every button; switch active device; remove device; settings toggles; switch-key recorder; reset settings", "status": "done", "note": "All done live (see Track B4/B5). Switch verified both directions; remove verified; every toggle/segment/chip verified firing a real command and reflecting the result." },
        { "id": "D5", "title": "Test all shutdown paths (window close, tray Quit, Ctrl-C); confirm no lingering processes", "status": "done", "note": "dev window X -> exit 0; prod window X -> hide to tray; tray 'Quit Flow' -> exit 0, daemon survives; flutter run 'q' -> exit 0. Daemon: clean exit 0 under termination (verified via `timeout` = SIGTERM in the first smoke test); the tokio::signal::ctrl_c handler is the standard break-the-loop path. Hand-injecting a Windows CTRL_C_EVENT into a background daemon from another console is unreliable and was not used. No lingering processes / ports across dozens of start/stop cycles this session." },
        { "id": "D6", "title": "Record a GIF of the working pairing + switching flow", "status": "done", "note": "scratchpad/flow_working.gif (also copied to Desktop/flow_working_demo.gif): 18 frames, onboarding -> dashboard -> settings sections -> Pair New Device -> Found -> connected -> Switch -> Remove -> daemon-killed -> auto-reconnected." },
        { "id": "D7", "title": "OPTIONAL: repeat pairing across a second physical machine on the same LAN", "status": "pending", "note": "NOTE: a real device named 'MacBook' (os: macos) running flow-daemon was discovered on the user's LAN during testing and appeared as a live pairing candidate in the UI. Not paired with it (didn't want to touch the user's other machine unprompted). The cross-machine pairing path is the same one already proven with Flow-B; completing it against the real Mac just needs the user to press 'Pair a device' on the Mac too." }
      ]
    },
    {
      "id": "E",
      "title": "Regression gate",
      "status": "done",
      "tasks": [
        { "id": "E1", "title": "cargo test --workspace + cargo clippy --workspace --all-targets -- -D warnings + cargo fmt --check", "status": "done", "note": "All green. 136 daemon lib tests + integration/doc tests pass (was 134 pre-task; +2 new for remove_device link-state and platform permission). Fixed a pre-existing Windows-only clippy -D warnings failure." },
        { "id": "E2", "title": "flutter analyze + dart format --set-exit-if-changed + flutter test", "status": "done", "note": "flutter analyze: no issues. dart format: clean (also formatted 2 pre-existing unformatted files). flutter test: 65 passed. e2e (test/e2e/daemon_ui_flow_e2e_test.dart): 7/7 passed after fixing it to pin the daemon's data dir + token path via FLOW_DATA_DIR/FLOW_IPC_TOKEN_PATH (it was pre-existing broken on Windows because `directories` ignores a redirected HOME there)." }
      ]
    }
  ]
}
