# Rust
/target/
**/target/

# Flutter / Dart (flutter/.gitignore, from `flutter create`, covers the rest)
flutter/**/generated_plugin_registrant.dart

# Editors / OS
.DS_Store
.vscode/
.idea/

# Claude Design export (reference only, not project source) — tracked
# only on the product/design branch, ignored everywhere else.
/design/claude-design-export/
